import Link from 'next/link'

export default function Layout({ children }) {
  return (
    <main className="min-h-screen flex flex-col bg-black text-white">
      {/* Nav */}
      <nav className="flex gap-6 p-4 bg-gray-900">
        <Link href="/">Home</Link>
        <Link href="/about">About</Link>
        <Link href="/festival">Festival</Link>
      </nav>

      {/* Page content */}
      <div className="flex-grow">{children}</div>

      {/* Footer */}
      <footer className="p-4 bg-gray-900 text-center text-sm text-gray-400">
        ✨ Dream Market Covenant — Every commit is a blessing, every deployment a revelation ✨
      </footer>
    </main>
  )
}
